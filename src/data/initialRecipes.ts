import { Recipe } from '../types';

/**
 * Empty initial heirloom recipes array.
 * Recipes are now loaded directly from the user's Google Drive or added manually.
 */
export const INITIAL_HEIRLOOM_RECIPES: Recipe[] = [];
